---
'@aws-amplify/backend': patch
---

Bump `@aws-amplify/backend` alongside the `@aws-amplify/backend-auth` `manageProviderForUser` change so the umbrella package stays version-consistent with its sub-dependencies (satisfies `check_changeset_completeness`).
