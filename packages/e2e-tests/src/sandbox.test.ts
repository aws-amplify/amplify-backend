import { describe, it } from 'node:test';

describe('amplify sandbox', () => {
  it('deploys fully loaded project and generates expected client config', async () => {
    // setup project template
    // run amplify sandbox
    // wait for success/fail
    // evaluate post assertions
  });
});
