import { describe, it } from 'node:test';

describe('fetchNearestPackageJson', () => {
  it('returns package.json in starting directory', async () => {
    /* TODO */
  });
  it('returns package.json in dir chain', async () => {
    /* TODO */
  });
  it('returns package.json in root dir', async () => {
    /* TODO */
  });
  it('fails if no package.json found in dir chain', async () => {
    /* TODO */
  });
});
