/**
 * Used to run `create amplify` at a specific tagged release
 */
export const amplifyAtTag = 'amplify@latest';

/**
 * Environment key for SQS Queue URL
 */
export const sqsQueueUrlKey = 'SQS_QUEUE_URL';
