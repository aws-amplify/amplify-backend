/** Canonical hosting output directory name. */
export const HOSTING_DIR = '.amplify-hosting';

/** Static assets subdirectory within the hosting output. */
export const STATIC_DIR = 'static';

/** Deploy manifest filename. */
export const MANIFEST_FILENAME = 'deploy-manifest.json';
