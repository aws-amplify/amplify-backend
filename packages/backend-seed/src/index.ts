/*import { Secret } from '@aws-amplify/backend-secret';

//ultimatly want the API to look something like this: getSecret(secretID)
export const getSecret = (secretName: string): Promise<Secret> => {
    //somehow need to get the secret from the secret manager
};
*/
